id: len-add-to-checked
snapshots:
  ? |
    void f(nxt_buf_t *a, nxt_buf_t *b) {
        size_t total;

        total = a->len + b->len;
    }
  : labels:
    - source: total = a->len + b->len;
      style: primary
      start: 60
      end: 84
