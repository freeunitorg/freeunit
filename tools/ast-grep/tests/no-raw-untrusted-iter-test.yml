id: no-raw-untrusted-iter
valid:
  - |
    void copy(char *dst, char *src, size_t n) {
        memcpy(dst, src, n);
    }
invalid:
  - |
    void copy(char *dst, char *src, size_t n) {
        while (n--) {
            *dst++ = *src++;
        }
    }
